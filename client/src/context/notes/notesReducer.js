import {
  GET_NOTES,
  GET_NOTE,
  CREATE_NOTE,
  UPDATE_NOTE,
  DELETE_NOTE,
  FILTER_NOTES
} from "../types";

export default (state, action) => {
  switch (action.type) {
    default:
      return state;
  }
};
